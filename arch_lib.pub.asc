# arch_lib 仓库签名公钥

此仓库尚未配置 GPG 签名。

要启用签名，仓库所有者需在本地运行一次：

```bash
bash scripts/generate-signing-key.sh
```

然后：
1. 把生成的 `gpg-private-key.asc` 内容添加到 GitHub Secret `GPG_PRIVATE_KEY`
2. 用生成的 `arch_lib.pub.asc` 替换本文件并提交

启用后，每次构建的包和数据库都会带 `.sig` 签名，用户可导入公钥并设置 `SigLevel = Required DatabaseOptional` 验证。

在此之前，请使用 `SigLevel = Optional TrustAll`。
